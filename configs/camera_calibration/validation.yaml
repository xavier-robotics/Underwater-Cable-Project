mode: validation

intrinsics_file: ../../outputs/camera_calibration/intrinsics/intrinsics.yaml
extrinsics_file: ../../outputs/camera_calibration/extrinsics/extrinsics.yaml
housing_file: ../../outputs/camera_calibration/housing/housing.yaml

target:
  type: checkerboard
  inner_corners_cols: 8
  inner_corners_rows: 6
  square_size_m: 0.05
  target_origin: first_inner_corner

# Add independent input blocks below to perform data-based validation. When omitted,
# validate still checks file versions, units, models, frames, transforms and runtime rays.
#
# intrinsic_validation:
#   input: {type: video, path: ../../data/calibration/validation/air.mp4, frame_interval_sec: 0.5}
# extrinsic_validation:
#   datasets: []
# housing_validation:
#   input: {type: video, path: ../../data/calibration/validation/water.mp4, frame_interval_sec: 0.5}
#   minimum_improvement_percent: 5.0

output:
  directory: ../../outputs/camera_calibration/validation

